# Grafana Dashboard Provisioning
apiVersion: 1

providers:
  - name: 'ParkFlow'
    orgId: 1
    folder: 'ParkFlow'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
